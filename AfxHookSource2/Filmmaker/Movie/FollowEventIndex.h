#pragma once

#include "FollowCamera.h"

// Background .fmjson loadout-event loading + indexing for the Follow Camera (weapon/C4 drop and
// pickup events used by Preview Tick / SelectEvent). Split out of FollowCamera.cpp; declarations
// live on the FollowCamera class itself in FollowCamera.h.
